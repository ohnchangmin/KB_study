<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
	<select id="select" resultType="com.cm.org1.MemberDto">
		select * from member
	</select>
	<!-- <insert id="insert" parameterType="java.lang.String"> insert into test 
		values (#{para1}, "1234") </insert> -->

	<insert id="insert">
		insert into member (id, pw, age, gender)
		values
		('aa', 'aa', 12, 'male');
	</insert>
</mapper>