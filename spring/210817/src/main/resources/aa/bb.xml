<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="test">
	<select id="select">
		select * from test
	</select>
	<!-- <insert id="insert" parameterType="java.lang.String"> insert into test 
		values (#{para1}, "1234") </insert> -->
	<!-- ctrl+shift+f 자동정 -->

	<insert id="insert" parameterType="com.cm.org.dto.TestDto">
		insert into test values (#{para1}, #{para2})
	</insert>
</mapper>
